# Server Configuration
# Use PORT provided by Render (or default to 8080 locally)
server.port=${PORT:8081}

# PostgreSQL Database Configuration (Render - fixed values)
spring.datasource.url=jdbc:postgresql://dpg-d39pph56ubrc73eat090-a.singapore-postgres.render.com:5432/venue_af2j?sslmode=require
spring.datasource.username=venue_af2j_user
spring.datasource.password=2cZUiYfMOObFgLqVOMjczMy3TPuFrmHR
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Connection Pool (HikariCP)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=1800000

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.com.example.venue=DEBUG

# SQL initialization (disabled to prevent data.sql from running on every restart)
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=false